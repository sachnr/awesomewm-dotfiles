### **Dependencies**

- mpc-cli for mpd
- ffmpeg for getting cover art from music files
- `wpctl` and `pactl` for controlling audio

### Bar style

in `rc.lua` change

```lua
require("bar").setup({
    style = "vertical",
})
```
